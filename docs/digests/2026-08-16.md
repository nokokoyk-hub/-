# 作業ダイジェスト 2026-08-16（JST）

## ひとことで言うと

Codexクラウドの共通「ちゃぴ」設定キットをmainへ反映し、設定リポと `ojuken-manager` のクラウド環境へ実登録した。

## 確認したこと

- 設定リポの既定ブランチは `main`
- 既存のルート `AGENTS.md` には作業ルールだけがあり、ちゃぴ人格の定義がなかった
- `codex-kit/` はREADMEで「今後追加予定」とされ、まだ存在しなかった
- 公式仕様では、Codexは Codex home の `AGENTS.md` をグローバル指示として読み、その後に各リポの `AGENTS.md` を重ねる
- Codexクラウドはリポをチェックアウトした後に環境のセットアップスクリプトを実行し、キャッシュ再開時は任意のMaintenance scriptを実行する

## 変更内容

### 新設

- `codex-kit/global/AGENTS.md`
  - 自然な関西弁のちゃぴ人格
  - 毒舌の矛先、初心者向け配慮
  - 正本の役割分担、安全、作業前後のルール
- `codex-kit/setup.sh`
  - 共通設定を Codex home の `AGENTS.md` へ導入
  - 取得失敗・空ファイル・想定外ファイルを検知
  - 既存設定は上書き前にバックアップ
- `codex-kit/README.md`
  - CodexクラウドのSetup / Maintenance scriptへの登録方法
  - 新しいチャットでの確認方法と注意点

### 更新

- ルート `AGENTS.md`：Codex共通設定を最初の読込対象へ追加
- `README.md`：Codexキットの入口を追加
- `docs/current_state.md`：原因・対応・反映条件・残タスクを更新
- `docs/lessons.md`：設定は「正本・配布・実測」の3点セットという学びを追加

## 変更しなかったもの

- `claude-kit/`
- 各プロダクトリポジトリ

## 確認結果

- 変更ブランチから全変更ファイルを再取得し、内容とリンク先を確認
- セットアップスクリプトは失敗時に停止する構成
- Windows上のBash実行環境は権限制約で起動できず、ローカルの `bash -n` は未実施
- PR #10をmainへSquash mergeし、commit `59491f8c` と配布元raw URLの内容を再確認
- 設定リポ `nokokoyk-hub/-` 用のCodexクラウド環境を作成し、Setup / Maintenanceの両方に導入コマンドが保存・表示されることを確認
- 敬語だった新規タスクは `ojuken-manager` で起動しており、設定リポ環境のタスク数は0だったため、リポ単位の環境違いが原因と特定
- `ojuken-manager` 用環境を作成。Setupは `npm ci` とちゃぴ導入、Maintenanceはちゃぴ設定更新だけに分け、既存の依存関係導入を維持
- 正しい `ojuken-manager`・`main` の新規タスク画面まで選択確認済み。新しいタスクはまだ送信していないため、実際の口調確認は未実施

## changelog / version

- changelog：このリポに専用ファイルがなく、今回の内容は設定キットと文書の追加なので新設しない
- version：バージョン番号を持つファイルがないため更新しない

## 次に必要なこと

1. `ojuken-manager` 環境から新しいクラウドタスクを開始し、「ちゃぴ」の関西弁と読み込んだ指示元を実測確認
2. その他の開発リポでも使う場合、同じ導入コマンドを各リポ環境へ横展開
