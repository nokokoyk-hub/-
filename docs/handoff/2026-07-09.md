# 引き継ぎメモ 2026-07-09（JST）

## 次回やること（優先順）

1. **のんの宿題の確認**：Web版チャットのプロジェクト指示欄に `claude-kit/web-chat/PROJECT_INSTRUCTIONS.md` を貼ったか聞く。貼った後の Sonnet 5 の挙動（人格拒否・無関係な差し込み・メモリ書き換え）が改善したかヒアリング。改善不十分なら指示文の強化版を作る
2. **学びの記録システムの初運用を見守る**：各プロダクトリポで最初の `/wrapup` 時に lessons.md が正しく生成されるか。生成されたら書きぶり（理由・日付・出典つきか）を確認
3. のんが新サイトを作りたがっている（雑談レベル）。話が出たら、新リポ作成 → 北極星整備 → 開発の流れで対応

## 作業途中のもの

- なし。PR #2（Web版チャット指示文）・PR #3（学びの記録システム）はどちらもマージ済み。配布故障も修理・実証済みで、やりかけの作業はない

## 注意点（ハマりポイント）

- **このリポは公開（public）**。配布（raw URL への curl）がそれを前提にしているので、**非公開に戻すと配布が静かに死ぬ**。非公開化の話が出たら必ず配布方式の変更とセットで
- 2026-07-09 の修理より前に作られたセッションにはキットが入っていない（/kickoff が Unknown command になる）。古いセッションは直さず、新セッションを立て直すのが正解
- 環境は「デフォルト」1個のみ。環境を増やしたら、そのセットアップスクリプトにも curl の1行が必要
- のんは開発初心者。専門用語は噛み砕き、Git 操作等はちゃぴが実行して報告する

## 次セッション冒頭用の呼び出しコピー

> /kickoff して。前回（7/9）は設定キットの大整備が完了：Web版チャット用指示文とlessons.md学びシステムをマージ、配布故障（リポ非公開→404）も修理済みや。今日はWeb版チャットに指示文を貼った結果の確認からやで
