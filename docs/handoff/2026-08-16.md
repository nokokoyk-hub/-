# 引き継ぎメモ 2026-08-16（JST）

## 今回の成果

Codexクラウドの応対が堅い原因を設定リポで特定し、Codex専用の共通ちゃぴ設定キットを作成した。

- PR #10：mainへSquash merge済み（commit `59491f8c`）
- 正本：`codex-kit/global/AGENTS.md`
- 導入：`codex-kit/setup.sh`
- 手順：`codex-kit/README.md`
- 設定リポ `nokokoyk-hub/-` 用のCodexクラウド環境を作成済み
- 実測対象の `nokokoyk-hub/ojuken-manager` 用環境も作成済み
- `ojuken-manager` のSetupは `npm ci` → ちゃぴ導入、Maintenanceはちゃぴ設定更新のみ。依存関係導入を維持した構成で保存・表示確認済み
- Codexクラウドの「設定 → 一般 → カスタム指示」へ、アカウント全タスク共通の短いちゃぴ人格を保存。成功通知と再読み込み後の保持を確認済み
- ルート `AGENTS.md` は、共通設定を最初に読むよう更新済み
- `claude-kit/` は変更していない

## 原因

既存のルート `AGENTS.md` はこの設定リポの作業ルールだけで、関西弁のちゃぴ人格がなかった。
また、リポ直下の `AGENTS.md` は他リポへ自動配布されないため、全リポ共通設定には Codex home の `AGENTS.md` とクラウド環境からの導入が必要。

## 次にやること

1. アカウント共通カスタム指示の保存後に、新しいCodexクラウドタスクを開始する
2. 新しいタスクで次を送る

> 今読み込んでいる指示元を教えて。自己紹介もして。

3. アカウント共通カスタム指示と各リポの `AGENTS.md` が両立し、自然な関西弁の「ちゃぴ」で返ることを確認
4. 実測結果を `docs/current_state.md` と次回ダイジェストへ反映
5. 実測成功後、環境Setupの重複人格配布を残すか簡素化するか判定

## 注意点

- PR #10はmainへ反映済みで、配布元raw URLも再取得確認済み
- 敬語だった「UIを洗練させる」タスクは `ojuken-manager` で起動していたが、専用環境作成前のタスクなので設定は遡及しない。必ず新しいタスクで確認する
- プロジェクト側の `AGENTS.override.md` や深い階層の指示が口調を上書きしていないかも確認する
- アカウント共通の口調・人格は一般設定のカスタム指示へ置く。各リポ環境は依存関係・ツール設定用で、リポ固有ルールは各 `AGENTS.md` を正本にする
- 旧OneDrive Vaultは引き続き凍結し、存在確認を含めて触らない
