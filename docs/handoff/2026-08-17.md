# 引き継ぎメモ 2026-08-17（JST）

## 今回の成果

Codexが依頼外の作業で使用量を使い切る暴走の原因を特定し、共通設定に暴走ガードを追加した。

- ブランチ：`claude/codex-agent-overrun-5vlmt1`
- 原因：`codex-kit/global/AGENTS.md` に「やること」だけあって「止まりどころ」（スコープ上限・停止条件・リトライ上限）がなかった
- 対応：「作業範囲と止まりどころ（最優先）」セクション追加、終了時の締め作業を明示依頼時のみに変更、READMEへ「暴走したときの止め方」追加

## 次にやること

1. **暴走中のCodexタスクが残っていれば、Codex側の画面でStop / キャンセル**（設定変更では止まらない）
2. このPRの差分をのんが確認 → mainへマージ
3. Maintenance script 登録済み環境は次回実行で反映。未登録環境は Setup script を実行
   ```bash
   curl -fsSL https://raw.githubusercontent.com/nokokoyk-hub/-/main/codex-kit/setup.sh | bash
   ```
4. **新しい**Codexチャットで軽い依頼（例：READMEの誤字1個直して）を投げ、依頼外の作業へ広がらず、終わったら止まることを実測確認
5. Codexが勝手に進めた作業のPR・差分を確認し、要らない変更が混ざっていないか棚卸し
6. 実測結果を `docs/current_state.md` と次回ダイジェストへ反映

## 追記（同日2回目）：Codexクラウド環境設定

- `codex-kit/environments/ojuken-manager.md` を作成。のんがCodexクラウドの環境設定画面に貼るだけの形
- のん側の作業：①環境設定画面を開く → ②Setup / Maintenance script を上記ファイルの内容へ差し替え → ③キャッシュリセット → ④新タスクで `npm test`＋`npm run build` を依頼して実測確認
- 現状のCodex環境設定がどうなっているかは未確認（ちゃぴからは見えない）。のんのスクリーンショットで突き合わせるのが最速

## 注意点

- 新設定は「mainマージ＋スクリプト実行後の新しいチャット」から有効。古いチャット・実行中タスクには効かない
- `AGENTS.md` は指示であって強制ではない。使用量の消費が心配なら、Codex側の環境設定・タスク実行の上限も併せて確認する
- 前回（2026-08-16）のCodexちゃぴ人格設定のマージ・登録タスクは未完なら継続。今回のガードは同じファイルへの追記なので、マージ順は「先に入った方へ追従」で問題ない
